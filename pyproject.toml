[project]
name = "searxngr"
version = "0.1.0"
description = "SearXNG from the terminal"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.32.3",
    "rich>=14.0.0",
]

[project.scripts]
searxngr = "searxngr:main"
